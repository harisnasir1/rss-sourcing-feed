# Local environment for dev — DO NOT commit this file
# Set the RunPod origin so Vite's dev proxy can forward /api/runpod/* requests.
VITE_RUNPOD_URL=https://rmizhq2lxoty3l-4000.proxy.runpod.net/api/product/getlisting
# Optional API key (leave blank if not needed). Do NOT commit real keys to git.
VITE_RUNPOD_KEY=
