# Example environment variables for Vite
# Put your real values in a .env.local or .env file (do NOT commit secrets).
# Base endpoint; the app appends ?search=<term> at runtime
VITE_RUNPOD_URL=https://rmizhq2lxoty3l-4000.proxy.runpod.net/api/product/getlisting
# Optional API key for authenticated endpoints
VITE_RUNPOD_KEY=
# Leave the search box empty to fetch all items (no ?search parameter)
