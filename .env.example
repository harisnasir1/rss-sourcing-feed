# Example environment variables for Vite
# Copy this file to .env.local and fill in values. Do NOT commit real secrets.

# Base endpoint (no default query string)
VITE_RUNPOD_URL=https://rmizhq2lxoty3l-4000.proxy.runpod.net/api/product/getlisting

# Optional API key for authenticated endpoints. If provided, the app
# adds both Authorization: Bearer <key> and x-api-key: <key> headers.
VITE_RUNPOD_KEY=

# Leave the search box empty to fetch all items. The app won't append
# a ?search parameter by default.
